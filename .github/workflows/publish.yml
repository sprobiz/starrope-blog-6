name: 📅 Scheduled Post Publisher

on:
  schedule:
    # KST 08:00 = UTC 23:00 (previous day)
    - cron: '0 23 * * *'
    # KST 16:00 = UTC 07:00
    - cron: '0 7 * * *'
  # Enable manual run button in Actions tab
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📝 Run publish script
        run: python scripts/publish.py

      - name: 📊 Check for changes
        id: changes
        run: |
          git diff --quiet && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: 🚀 Commit and push published posts
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Starrope Auto Publisher"
          git add -A
          git commit -m "📅 Auto Publish: $(TZ='Asia/Seoul' date '+%Y-%m-%d %H:%M KST')"
          git push
